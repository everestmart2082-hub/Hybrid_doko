# New Quixo server — uses same API as old server, different DB
PORT=5000
MONGODB_URI=mongodb://localhost:27017/quixo-new

# Reuse old server env for JWT, etc. (optional; copy from quixo/.env.example if needed)
# MONGO_URI=mongodb://localhost:27017/quixo-new
# JWT_SECRET=
# JWT_ADMIN_SECRET=
# REDIS_URL=redis://localhost:6379
